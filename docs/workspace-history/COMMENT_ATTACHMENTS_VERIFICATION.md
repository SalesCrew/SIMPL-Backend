> Historical workspace note copied before the Trello migration on 2026-08-31. Earlier deployment addresses and test counts below describe the time of that note; the current app is https://get-simpl.vercel.app.

# Comment attachments verification — 2026-08-31

- Applied additive `comment_attachments` migration to the existing Trello+ Supabase project (`xqvexzoswhraqicbmckj`). No keys added to source. Frontend/backend changes are local, not redeployed by this task.
- Live API/Storage checks with disposable accounts: PNG/PDF/XLSX/opaque uploads; drafts invisible to teammates, unprovisioned users and anonymous clients; foreign-owner/cross-card/duplicate/empty submission denial; atomic attachment-only message publication; recipient notification; authenticated downloads; relink denial; immediate denial after deactivation; protected published files during draft cleanup; undo plus actual Storage deletion; expired-draft cleanup; ten-file server limit. The test removes only its own records/accounts/files.
- Transactional SQL test passed: an isolated-workspace member cannot read the other workspace's comments or attachment metadata; an administrator can. All fixtures rolled back. Existing authenticated Storage policy consults the same metadata RLS and still forbids public/signed links.
- Supabase security advisor returned no lints after the migration.
- Browser testing used an isolated local fixture rendering the actual CardEditor, CommentComposer, shared attachment tiles and lightbox. Synthetic paste/drop events delivered generated files through the real event handlers; the operating-system file chooser was not automated. Tested mixed-image/document message send, image tab, lightbox, successful clipboard copy, image/document download actions, size rejection, failed-send draft preservation and retry. Browser checks use device-local storage; the real API path is covered independently above.
- At 390×480, found and fixed a long-text/multiple-file composer overflow. After the fix the composer bottom was 435.2 px inside the modal body's 449.2 px bottom, with a bounded scrollable draft area. Restored viewport override and closed/deleted the temporary QA page.
- React review: shared preview code; callback-ref resize observer cleanup; object URL revocation; IME-safe Enter handling; duplicate-send guard; keyboard-operable gallery tabs; reduced-motion styles; no active document embedding.
- Regression verification: frontend/backend production builds and full Vitest suites (127 frontend + 34 backend tests) passed during implementation. Dedicated repeatable tests are `frontend/src/comment-attachments.test.ts`, `backend/scripts/verify-comment-attachments.ts` and `backend/sql/verify-comment-attachments.sql`.

Limitations: 20 MB per file, ten per message. Unknown formats are downloads only; no Office/PDF editor, antivirus scanner, or external preview service. Abandoned remote drafts are cleaned on a later upload/operator cleanup, not by a newly introduced scheduler.
