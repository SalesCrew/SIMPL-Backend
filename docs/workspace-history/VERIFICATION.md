> Historical workspace note copied before the Trello migration on 2026-08-31. Earlier deployment addresses and test counts below describe the time of that note; the current app is https://get-simpl.vercel.app.

# Verification — 2026-08-31

## Conditional soft overlap edge — latest local update

- Added a short alpha-feather to the moving columns at their clipping boundary. It begins only after the boundary crosses the columns' 17px top padding and grows smoothly to 16px. At rest the gradient has zero width, so the original top edges remain intact. The existing clipping boundary still prevents cards from covering the unboxed filters.
- The effect is a transparency mask on the columns, not a backdrop-blur overlay: the stationary mountain image and controls are untouched. Expanded mask bounds preserve surrounding shadow/focus space, and forced-colors mode retains the hard clip without fading. [CSS gradient masks](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-image), [mask clipping](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-clip).
- Browser checks in the isolated localhost preview: zero-width mask at scrollTop=0; at scrollTop~274px the mask fades from 124.064px to 140.064px (16px) at the existing cut line; returning to the top restores zero width. Screenshots confirmed the softened overlap and unchanged artwork. Hero filter and mask both remained `none`; browser error/warning log was empty. No records were saved, and the test tab was closed.
- **86 frontend tests passed**; TypeScript and production build passed (Vite 3.05 seconds). CSS-only change, no new scroll listeners, React state, dependencies or backend changes. Available in the local preview; no production deployment in this pass. Cross-browser/mobile and forced-colors rendering were not visually exercised.

## Layered hero scrolling — latest local update

- Reverted the rejected frosted filter panel and enlarged/re-cropped backdrop before implementing the revised behavior. Preserved the original hero image, crop, 72px fade and header/control spacing.
- `BoardViewport` provides one native vertical/horizontal scroll surface. The title, subtitle and contributor avatars scroll out beneath the page header. The existing filter controls rise and remain available without an added panel, background fill, blur, border or shadow.
- Layers are deliberately independent: the board surface scrolls at level 0, the stationary hero artwork sits at level 1, and project columns/cards sit at level 2. Controls sit above them at level 3. The cards' clipping edge keeps the transparent control area clear, without painting a replacement toolbar surface.
- Hero height is measured from the actual heading and controls, including font/layout changes. A cleaned-up ResizeObserver handles size changes; a passive, animation-frame-coalesced scroll listener updates only a CSS clipping variable, without React renders per scroll frame. [ResizeObserver reference](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver), [sticky positioning behavior](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position).
- Browser checks in the isolated localhost demo: original appearance at the top; scrolling to the bottom cards; returning to the top; horizontal column scrolling; direct pointer opening of the pinned filter; filtering down to two completed cards and resetting to fourteen. At scrollTop ~274px the hero stayed at y~78px, the intro's bottom moved above the viewport, controls stayed at y~90px, and the board surface moved to y~20px beneath the image. Screenshots confirmed columns visibly cross over the unchanged artwork and the filter area remains unboxed. Direct pointer opening of the pinned filter preserved scrollTop; the semantic automation click instead scrolls sticky controls into view, so it was not used as evidence for pointer behavior.
- **86 frontend tests passed**, including three new viewport structure/decorative-content checks and the shared board-dragging regression suite. TypeScript and production build pass (local Vite build 3.06 seconds); the earlier unrelated board typing error is now resolved in the shared files. React review covered observer/listener cleanup, transient DOM updates, existing focus/portal behavior and reduced-motion rules. No dependencies or backend/database changes.
- This is a **local-preview update**, not a new production deployment. No records, attachments or workspace settings were saved; view filters were reset after verification. Full mobile, cross-browser and keyboard-only audits are not claimed.

## Scrollbar containment — latest local update

- Shared scrollbars now use an 8px transparent rail with a slim, rounded thumb, inset track ends and coordinated sage hover/active colors. Textarea track ends use the 13px field radius. Existing intentionally hidden sidebar/comment-composer scrollbars remain hidden; native scrolling and Radix select scroll controls remain in place.
- Dialogs now clip their rounded outer shell and scroll a separate inner body, inset 6px from the right/bottom edges. The header and close control stay outside that scroll viewport. Workspace/comment lists reserve consistent gutter space; notification lists receive the same inset treatment and their panel is bounded to the viewport height. No scroll-position JavaScript, dependencies, backend or database changes.
- Browser verification in the isolated localhost demo: card dialog at top and bottom, long unsaved description text, custom card dropdown, workspace switcher, notifications empty state, compact project dialog and horizontally overflowing board. The card shell stayed at scrollTop=0 while its inner body scrolled about 264px; the header stayed at the same screen position. The inner right/bottom edges measured about 7px inside the outer border. Screenshots confirmed the thumb clears rounded corners. Description text was restored without saving; no records were created or changed. The agent-created test tab was closed.
- **55 frontend tests passed**, including three new dialog-structure/close-state checks. Hydrated browser markup retains the dialog title/description associations. Local browser error/warning log was empty. React review kept Radix focus/close behavior and existing reduced-motion rules intact.
- Compatibility: WebKit scrollbar styling is paired with a standard thin-scrollbar fallback only where the pseudo-element is unsupported; non-auto standard properties can override WebKit styling in Chromium. Forced-colors mode uses system colors. [MDN scrollbar styling and precedence](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/::-webkit-scrollbar). Firefox, mobile, long notification/workspace lists and a full keyboard-only audit were not exercised in this pass.
- **Not deployed yet:** the first production build passed (local Vite build 3.03 seconds). A concurrent edit subsequently changed Board.tsx at 17:07:36; the shared build now reports TS2322 at line 138 because its drag listener is typed as `Function | undefined`, not a React keyboard handler. That unrelated edit was preserved. No failed build was deployed; the last production release below remains live.

## Form controls — preceding production update

- Used the existing comment composer as the visual reference. Shared tokens preserve its exact focus border **#9bb692**, shadow **0 3px 9px #536b4018**, and **13px** corner radius. The comment field's appearance itself remains unchanged.
- Inputs and textareas now use white surfaces, softer borders, consistent padding, hover/disabled treatment and the shared focus styling. The card title has an explicit, slightly stronger type treatment instead of the obsolete first-child selector. Search focuses its outer wrapper; number fields, account/login fields, label/project/workspace forms inherit the same tokens.
- Form dropdowns retain Radix behavior and receive polished option rows, soft project-color tiles, aligned avatars/empty-assignee icon, a quiet selected checkmark and softer floating-panel shadow. Explicit **filter** and **toolbar** variants keep board filters outside the redesign; portal styling is variant-scoped too.
- Browser checks in the isolated localhost demo: opened card project and member menus, confirmed option labels and selected state, Escape dismissal, and captured the revised card/dropdown appearance. Computed focus border, shadow and radius matched the comment reference exactly for title, description, open dropdown, search and numeric input. Filter triggers retained their original #fcfdfa background, #e3e9dc border and 11px radius. No cards, comments, assignments or workspace settings were saved during these checks.
- **52 frontend tests passed**, including five new select-variant/accessibility regressions; TypeScript and production build passed (local Vite build 3.63 seconds). React review retained accessible names, controlled values, disabled semantics, Radix keyboard handling and reduced-motion support. No new dependencies or backend/database changes.
- Production: **READY**, https://trello-plus.vercel.app, Vite/React. Deployment **dpl_DNzmX8Wd851M2LD9Pt1ZJWf8ZSd7**, https://trello-plus-92udsosgt-sales-crew.vercel.app. Remote full build duration was not returned; no commit SHA (repository remains uncommitted/unlinked).
- The production page exposes the expected new focus/radius tokens. Its existing browser session belongs to the previously removed QA account and correctly showed the access-disabled screen; no new login or account switch was attempted. Interactive form checks were performed locally.
- Local browser error/warning log was empty; Vercel production error-level scan over the last 10 minutes returned no logs. No continuous monitoring/drains were configured. Full mobile and keyboard-only audits are not claimed.

## Workspace confidentiality — preceding production update

**Story:** Admin workspace settings → atomic save RPC → live home-workspace authorization in Postgres/Storage → authorization-first board loading and filtered UI. Planned in WORKSPACE_ACCESS_PLAN.md before implementation.

| Boundary | Result | Evidence |
| --- | --- | --- |
| Admin UI | Passed for selective rules | Custom mode buttons and workspace checkboxes inspected visually; B displayed its symmetric A block; adding Shared through the production UI persisted the canonical B/Shared pair in SQL. Admin saw every workspace, including isolated ones. |
| Login/data loading | Passed | Real employee login started in A. Its switcher excluded B and isolated C, while Shared remained available. Five unit tests cover context-first loading, disabled accounts, admin-only rule reads, revision changes and fail-closed errors. |
| DB/RPC authorization | Passed | Real A/B/C/Shared fixtures: symmetric A/B denial, two-way full C isolation, shared-board non-bridging, administrator override. Direct reads/writes and move/completion RPCs could not access hidden data. Profiles, labels, comments, notifications and attachments were included. |
| Storage/backend | Passed | Hidden downloads, previously reserved uploads, signed URLs, reserve/finalize/card-delete routes denied. Hidden attachment DELETE is intentionally idempotent (204/no-op); SQL and Storage verified that it did not remove the object. Tested in-process against live Supabase and against the deployed backend. |
| Session/Realtime | Passed | Forged user metadata did not grant access. Existing JWTs lost access after home reassignment/deactivation. Publication contains only access_revisions; events contained only the recipient's ID and opaque counters, and publication DELETE is disabled. |
| Live UI revocation | Passed | Employee opened an allowed Shared card. Changing the test workspace's permission closed the dialog and returned the UI to A: dialogs=0, home selector=1, hidden card=0. |

- **79 unit tests pass**: 47 frontend + 32 backend. Strict TypeScript checks and frontend production build pass. Existing production auth/card/completion/comment/Realtime integration and attachment regression suites also pass after the RLS changes.
- SQL migrations: 20260831143536_workspace_isolation; 20260831144021_workspace_revision_signal_fix; 20260831144045_workspace_admin_visibility. Two initial live-test failures were fixed before verification: the safeupdate guard required an explicit predicate in the revision trigger; new-workspace upserts/seeding required an explicit admin SELECT override because the statement's cached workspace IDs predate the insert. Old permissive member policies were replaced, not left alongside restrictions.
- Labels now belong to one workspace. Backfill preserves original IDs in the original workspace and clones only labels actually used by cards in another workspace, preserving card associations. Unused confidential labels are not copied to other teams. New workspaces receive generic starter labels.
- React review: hook order remains stable; selection is tied to user identity; permission/account changes discard the board and unmount dialogs, aborting uploads/revoking object URLs. Independent row queries remain parallel, intentionally bracketed by authorization checks. Custom controls have pressed/checked states, focus styles, and reduced-motion support. Client Supabase requests use no-store.
- **Verification limits:** two additional browser actions were rejected by the browser safety mechanism (saving full isolation through the UI and switching to another QA employee). They were not bypassed. Full isolation and account reassignment were already verified through the real database/API suite; an additional full-isolation UI save/login round-trip is not claimed. No full mobile or keyboard-only audit was performed.
- Supabase security advisor: no table/RLS/definer-function warnings; a separate Auth warning reports disabled leaked-password protection. This paid-plan feature was not enabled without a plan decision. [Supabase password-security guidance](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). Performance notices are informational unused indexes and fixed Auth connection allocation; indexes were retained for integrity/access paths. [Index guidance](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index), [production configuration](https://supabase.com/docs/guides/deployment/going-into-prod).
- Security boundary: new requests use current database rules, not client-selected workspaces or editable JWT metadata. Previously authorized downloads/copies/screenshots cannot be recalled. The browser-local demo demonstrates the behavior but is explicitly not a security boundary. Isolation controls were not applied to any real customer workspace; SalesCrew remains open until an admin configures rules.
- Cleanup: all temporary files, cards and Auth/profile records were removed through scoped APIs. Owner-only transactional cleanup 20260831144920 removed eight verified-empty QA workspaces, their labels, fixed columns and block pairs; the fixed-column validator was restored. Final SQL: one SalesCrew workspace (open), six columns, five labels, zero profiles/cards/attachments/Storage objects/block pairs. User preview data was not reset; only agent-created tabs were closed.

### Deployment result

- URL: https://trello-plus.vercel.app — **READY**, production, Vite/React.
- Deployment: dpl_DDQ95qGzQkT1xRMEkghSVNJrMDrz; https://trello-plus-evu7hpi1s-sales-crew.vercel.app. Unique deployment URL has Vercel login protection; live UI verification used the public production alias.
- Commit: unavailable; GitHub linking remains deferred. Local frontend build: 3.41 seconds; complete remote build duration was not returned.
- Backend code did not require a new deployment: existing production routes consult the newly restricted user-scoped RLS. Production API checks passed.
- Post-deploy observability: production browser error/warning log empty. Vercel error-level scans for both frontend/backend over the last 15 minutes returned no logs. Continuous monitoring/drains were not configured by this task.
- The broader original goal still needs the real administrator email addresses; this feature required no additional credentials from the user for its completed API tests.

## Card attachments — preceding production update

- Planned placement and lifecycle first in ATTACHMENTS_PLAN.md, then added an attachment section below card descriptions, multi-file input/drop handling, screenshot paste, lazy image thumbnails, full-size preview, image-to-clipboard conversion, downloads and custom removal confirmation. New cards stay open after creation. Board cards show attachment counts.
- Uploads show actual byte progress followed by server verification, with cancel/retry. Competing card mutations and closing are disabled during uploads. The dialog header stays visible while scrolling long lists; effects honor reduced motion, revoke object URLs and abort work on unmount.
- Applied migration **20260831141004_card_attachments.sql**. The private **card-attachments** bucket accepts up to 20 MB per object; server reservations are capped at 20 per card under a parent-row lock. Attachment rows have RLS and no direct client write grants. Storage permits only owned, unexpired reservations; shared-row locking serializes writes against deletion. Downloads require active provisioned membership and the authenticated GET operation. Overwrites, anonymous access, listing and signed-link creation are denied.
- Backend publication checks actual object size, canonical MIME type and basic byte signatures/UTF-8. This is not malware scanning or full document validation. Office and ZIP contents are never unpacked or executed; only raster images are previewed inline.
- Card deletion removes files through Storage's API before deleting metadata/card rows. A restrictive foreign key prevents bypass. Failed cleanup remains tracked. Expired reservations are swept on the next upload to that card; the operator cleanup command handles up to 100 globally. No separate recurring cleanup schedule was created.
- **67 unit tests pass**: 35 frontend (including 6 attachment-domain tests) and 32 backend (including 22 file-validation cases). Frontend production build and strict backend TypeScript checks pass.
- Live integration passed against both the local Express app with live Supabase and the **deployed production API**: reservation/upload/finalize/read, repeat completion, pending-file isolation, forged path/metadata denial, wrong-uploader denial, overwrite/signed-link/listing denial, anonymous/unprovisioned/deactivated denial, forged signatures, size mismatches, cancel and expired cleanup, restrictive deletion FK, file cleanup on card deletion, and 21 concurrent reservation attempts respecting the cap.
- A real **20 MB** file uploaded and finalized successfully through the deployed API; **20 MB + 1 byte** was denied by Storage itself. Boundary-test files were removed.
- Authenticated production browser test passed with disposable credentials: existing image preview, full-size preview, Copy image success, Ctrl+V into a card, progress -> server publication -> third thumbnail, cancel then retry -> fourth thumbnail, text-file download initiation, custom attachment removal, full card deletion and final sign-out. The final deployment retained the authenticated board configuration after excluding local env files from uploads. No browser errors/warnings.
- Isolated localhost demo browser test passed: create card -> remain in details, paste two screenshots, reload -> both persist, copy the persisted image, delete card and its attachments. The user's 127.0.0.1 demo was not reset.
- OS-native file-picker and OS-originated drag/drop gestures were not driven by browser automation. Their input handlers share the verified upload pipeline and were reviewed. Cross-browser/mobile/device-specific gestures remain unverified.
- Security advisor returned no lints. Final health is 200/configured=true, production CORS is allowed, no-store/nosniff remain present. Both Vercel error-level scans over the last 15 minutes returned no logs.
- Cleanup confirmed **zero live attachments, stored attachment objects, profiles and cards**. Only fixture-created records were removed; temporary credential/image files and QA tabs were removed. Unrelated workspace files were preserved.

### Latest deploy result

- Frontend: [trello-plus.vercel.app](https://trello-plus.vercel.app) — **READY**, production, Vite/React. Deployment **dpl_B1YoZkpmwiwuP1NSPSQcKBmkT81H**, [immutable build](https://trello-plus-2fk6hlxcj-sales-crew.vercel.app). Complete remote build duration was not reported; local Vite build took 3.26 seconds.
- Backend: [trello-plus-backend.vercel.app](https://trello-plus-backend.vercel.app/api/health) — **READY**, production, Express. Deployment **dpl_HpiCp7j9dRwajRyWZTPPjNW8FJqp**, [immutable build](https://trello-plus-backend-r7e9cwk1g-sales-crew.vercel.app). Remote build: 13 seconds.
- Commit: unavailable; GitHub linking is still intentionally deferred. An earlier backend candidate failed typechecking in the new QA script and never replaced production; the corrected build passed.
- Monitoring: no error logs in post-deploy scans; no continuous monitoring/drains configured.

## Passed

- Frontend production build and backend strict TypeScript check.
- 39 automated tests: 20 board-domain tests, 7 workspace tests, 2 palette tests and 10 backend validation/access tests.
- Live Supabase SQL authorization/trigger checks, rolled back after execution.
- Live integration against `https://trello-plus-backend.vercel.app`: real password login, admin-created employee, employee/anonymous admin-route denial, shared cards, custom labels, completion, preserved origin project, admin-only read receipts, comments, Realtime notification delivery, recipient isolation, mark-seen, immediate access revocation, and self-demotion protection.
- Integration cleanup confirmed: zero profiles, cards, comments, and notifications remain; only six starting columns and five labels remain.
- Browser interaction checks in an isolated local demo: create/edit a card, labels, comment with timestamp, complete/reopen, explicit read toggle, keyboard drag between columns, employee view, and mark-all-seen empty state.
- Follow-up browser check confirmed that the employee sees all six columns, cannot see the admin team control, and a new card defaults to SPARK with Anna Leitner as assignee for that employee profile.
- Production login page checked visually and through accessible page content. No production browser errors or warnings were reported.
- Deployed API health returns 200 and configured=true. The production frontend origin is allowed by CORS. Responses include no-store and nosniff.
- Supabase security advisor returned no lints. npm audits returned no vulnerabilities in the root, frontend, and backend installations.
- Actual backend secret was checked against the frontend build: no matches. Local credentials and Vercel project-link files are ignored by Git.

## Not yet performed

- Provisioning the two real administrator accounts: awaiting Kilian's and Philip's email addresses. This does not block feature development or tests using disposable QA accounts.
- Browser login using those real accounts. Authenticated browser interactions above used the isolated demo; the live authenticated workflow was tested through the deployed API and Supabase clients.
- Device-specific mobile testing and multi-user load testing.
- GitHub integration, intentionally deferred until the repository is supplied.

A development-only React hot-reload warning occurred while formatting the entry file; the production login check was clean. No production data was used in testing.

## Pastel palette and custom controls update

- Added ten colors for a total of 16, shared by label, project, and profile pickers. The original six color identifiers remain supported.
- Applied additive Supabase migration `20260831133011_expand_pastel_palette.sql`. All 16 enum values were inserted into labels in a test transaction and rolled back successfully. Security advisor returned no lints.
- The integration test against the local backend and real Supabase database passed, including saving a mint-colored account and lavender label. Its temporary accounts and data were removed.
- Browser verified custom member selection, card column/assignee selection, nested filter menus, arrow-key/Enter selection, Escape closing only the inner dropdown, reset-to-all behavior, column type choices, numeric stepper, role menu, and account switch.
- Browser counted 16 color swatches, created a lavender label in the isolated local demo, and reopened it with the same color selected.
- All visible native selects and native HTML title tooltips were removed. Hidden form-integration selects generated by Radix remain intentionally inaccessible and visually hidden; their UI is entirely custom.
- New dropdowns and tooltips share accessible Radix primitives; account switches use switch semantics, and numeric controls retain spinbutton semantics with custom steppers.
- Installing new UI dependencies caused a transient development hot-reload React error. A fresh page load reported no errors/warnings, and the user's existing preview still displayed the board correctly.
- Frontend/backend builds, all 20 tests at this stage and dependency audit passed. This update was initially local-only; it is included in the subsequent fixed-bucket production deployment below. The additive database migration is applied.

## Fixed status buckets update

- Applied migration `20260831134326_fixed_status_buckets.sql` without changing existing column IDs or deleting data. Each status kind is unique; fixed columns reject INSERT, UPDATE and DELETE through the app, including administrator requests. Status names are reserved.
- Transactional SQL verification passed: fixed-bucket rename/recolor/reposition/retype/delete/duplicate attempts fail; direct task creation in status buckets fails; ordinary project creation/edit/deletion still works. The entire test was rolled back.
- Live integration passed against both the local and deployed backends: manual movement into every project and In Arbeit leaves the task open; the new completion RPC always sends it to Fertig; concurrent/repeated checks are idempotent; reopening restores the original project. Existing auth, comments, Realtime and access-revocation checks also pass.
- Browser verification in the isolated local demo passed: board check -> Fertig, manual menu move -> In Arbeit with an unchecked state, detail check -> Fertig, reopen -> original project. Both fixed headers have lock indicators and no edit/add controls; project headers and their 16-color editors remain editable. New-task menus contain projects only.
- React review confirmed consistent hook ordering and controlled selection values, explicit non-submit status buttons, accessible pressed states, and no new data-fetch waterfalls.
- Builds and all 31 tests pass. The browser reported no errors or warnings, and the Supabase security advisor returned no lints.
- Backend production deployment: `dpl_AdwtJQrL9VaVmwYANUAX4fPFzqsU`, https://trello-plus-backend-ov5q7yn8f-sales-crew.vercel.app, aliased to https://trello-plus-backend.vercel.app (READY).
- Frontend production deployment: `dpl_ExEVEqJXf6nUX8D35P5f1U6NubC9`, https://trello-plus-ahn1nmv36-sales-crew.vercel.app, aliased to https://trello-plus.vercel.app (READY). Both deployments include the earlier palette/custom-control and login-copy changes.
- Post-deployment checks: production login displays the new German developer joke, with no browser errors/warnings. API health returns 200/configured=true; CORS permits the production frontend; no-store and nosniff remain present. The production integration test passes, including the new completion RPC and pastel profile/label values.
- Temporary live test accounts/cards/labels were removed; database counts confirm zero profiles/cards/comments/notifications, six columns and five labels. SQL-test changes were rolled back. Only agent-created browser QA tabs were closed; the user's tabs and data were not reset.
- No Git commit SHA is available: GitHub linking remains deferred. No continuous monitoring was configured. The wider goal still awaits the two real administrator email addresses, not additional technical verification.

## Multiple workspaces update

- Applied `20260831135418_multi_workspaces.sql`. Existing column IDs and board content are preserved in SalesCrew; cards and columns have a workspace ID, and profiles have a default workspace ID. Composite foreign keys and validation triggers reject mismatched project/workspace assignments. Labels remain shared.
- Each new workspace atomically receives Allgemein, In Arbeit and Fertig. The status buckets remain protected and unique within each workspace. The completion RPC chooses the Fertig bucket belonging to the task's workspace. Workspace creation/renaming is admin-only; all active members may collaborate across all workspaces.
- `backend/sql/verify-workspaces.sql` passed against live Supabase inside a rolled-back transaction: creation/renaming, seeded buckets, valid and invalid defaults, employee access to both workspaces, workspace-scoped completion/reopening, cross-workspace card-move denial, employee admin-action denial, and anonymous denial. The existing SQL regression suite passed too.
- All 39 automated tests and frontend/backend builds pass. The live integration passed against both local and deployed APIs, including saved default workspace/project values and rejection of nonexistent workspace assignments. All disposable test accounts and records were removed.
- Isolated browser demo checks passed: create Marketing QA, automatically open its new board, create/complete a task there, assign Anna's start workspace/project, switch to her perspective and verify automatic startup, switch freely to SalesCrew without admin controls, then follow a cross-workspace comment notification as Kilian. The notification switched the board and opened the correct task. Existing SalesCrew cards remained intact.
- Visual review confirmed the custom popover, active/default workspace indicators, counts and shared pastel palette. Workspaces are ordinary accessible buttons inside a Radix popover, with focus styling. Keyboard automation did not move DOM focus reliably in this browser session, so a full keyboard navigation test is not claimed. Mobile-device testing remains outstanding; the switcher is retained in the compact mobile sidebar.
- React review: no conditional hooks or mutable render-time state changes; workspace selection is tied to the current user and resets on sign-out/demo-account change. The board filters reset on workspace changes, with notification-target dialogs preserved. Parallel paginated loading now includes workspaces, and Realtime publishes workspace changes.
- Security advisor: no lints. Performance advisor returned two informational notices: the newly added index has not yet seen production use ([unused-index guidance](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index)), and Auth uses a fixed 10-connection allocation ([production configuration guidance](https://supabase.com/docs/guides/deployment/going-into-prod)). Neither required changing the current small workspace or discarding integrity indexes.

### Production deployment result

- Frontend: https://trello-plus.vercel.app — READY, production, Vite/React. Deployment `dpl_74n1NAXyqLdcVKwBGxPjwJwq2dMC`, https://trello-plus-lc2fy2o6b-sales-crew.vercel.app.
- Backend: https://trello-plus-backend.vercel.app — READY, production, Express. Deployment `dpl_2LcLH5jRMH7R8avvtsX53e1DvZuM`, https://trello-plus-backend-d7mmq2f18-sales-crew.vercel.app. Backend build reported 8 seconds; no complete frontend build duration was returned.
- Commit: unavailable; GitHub linking is intentionally still deferred.
- Post-deploy checks: API health 200/configured=true, production CORS allowed, production integration passes, login page loads, browser error/warning logs empty. Vercel error-level scans over the last 15 minutes returned no logs for either project. Continuous monitoring/drains were not configured by this task.
- Live cleanup leaves one real workspace (SalesCrew), six columns and five labels, with zero profiles/cards/comments/notifications. The additional workspace and task used for visual QA exist only in the isolated localhost demo. No user preview was reset; agent-created browser tabs were closed.
